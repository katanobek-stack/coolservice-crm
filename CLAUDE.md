# Инструкции для Claude Code

## Проект
CRM система для рефрижераторного сервиса RefServiceDV (Владивосток)
Репозиторий: coolservice-crm

## Правила работы
- Читай только папку src/, web/src/ и functions/src/
- Не читай node_modules, .git, dist, build, functions/lib
- После каждого завершённого модуля делай git push
- Используй Sonnet на Medium если задача простая
- В начале каждой сессии проверь последний коммит и продолжи

## Стек
React + TypeScript + Vite + Firebase + Tailwind CSS

## Структура
- web/src/features/ — модули CRM
- web/src/shared/ — общие компоненты
- web/src/app/ — роутинг и провайдеры
- functions/src/ — Cloud Functions (Node 20, TypeScript, firebase-functions v2). Секреты (TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID) — через `firebase functions:secrets:set`, не в коде. TELEGRAM_CHAT_ID — один id или несколько через запятую (личный чат + группа админов), фото шлются через sendPhoto/sendMediaGroup с caption (задача, клиент+авто, автор из createdByName, примечание из workComment)

## Текущий статус (июнь 2026)
- CRM задеплоена на GitHub Pages
- Адрес: katanobek-stack.github.io/coolservice-crm/app
- Старая HTML версия: katanobek-stack.github.io/coolservice-crm
- Светлая тема применена ко всем вкладкам (белый/светло-серый фон)

## Что нужно доделать
1. Кнопка "Выйти" видна на мобиле
2. Кнопка "Закрыть наряд" с полем суммы
3. Сервисные задачи (не связанные с ремонтом)
4. График выручки на дашборде
5. Мобильное меню — добавить скрытые вкладки

## История разработки и решённые проблемы

### Что уже сделано и как решали:
- Перенос с монолитного index.html на React+Vite+TypeScript
- Firebase подключён через web/.env (ключи не в коде)
- Деплой: GitHub Actions → GitHub Pages /app
- Тёмный дизайн применён через CSS переменные
- Роли: admin видит всё, manager без персонала, mechanic только свои заявки без финансов
- Кнопка выхода в мобильной навигации
- Голосовой агент FloatingMicButton: одно нажатие старт, второе стоп, 60 сек лимит
- Фреон автоматически создаётся при каждом ремонте
- Тип транспорта: только Рефрижератор/Кондиционер (убрать Авто)
- Фото к задачам: Камера + Галерея
- Закрытие наряда: форма появляется в Отчётах когда все задачи выполнены
- Детали ремонта: фото группируются по задачам а не общей пачкой
- Telegram-уведомления (functions/): новая заявка, новая задача, новое фото к задаче, закрытие заявки — триггеры onCreate/onUpdate на clients/{clientId}, т.к. repairs/tasks/photos хранятся как вложенные массивы, а не отдельные коллекции

## Текущие баги и что нужно доделать:

### Критические:
1. Не создаётся ремонт — при нажатии Сохранить в форме ничего не происходит
2. Фото авто не отображается у физ. лиц после загрузки (показывает иконку)
3. История авто при нажатии кнопки "История →" не работает

### Средние:
4. Поле кг фреона слишком широкое — уменьшить до 100px + кнопка сохранить рядом
5. Нельзя удалить лишние фото из задачи
6. Excel отчёт — один файл с листами: Ремонты, Расходы, P&L
7. Отчёт по расходу фреона с графиком по месяцам

### Дизайн:
8. Карточки заявок в работе — некрасивые, слипаются
9. P&L не видно в мобильной навигации

## Правила которые нельзя нарушать:
- Тип транспорта: ТОЛЬКО Рефрижератор / Кондиционер
- Фреон создаётся АВТОМАТИЧЕСКИ при каждом ремонте
- Перед пушем всегда grep .split() .map() на undefined
- Дизайн — светлая тема, белый/светло-серый фон